# trazabilidad-hibrida

Para correr el backend: en la carpeta backend: - venv\Scripts\activate
                                             : - uvicorn main:app --reload

Para correr el frontend: En la carpeta frontend: npm run dev